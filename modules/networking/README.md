# networking

VPC + subnets + security groups for the RS Intelligence staging environment.
Populated in **T052**.

Planned outputs: `vpc_id`, `public_subnet_ids`, `private_subnet_ids`,
`alb_sg_id`, `ecs_sg_id`, `rds_sg_id`.
