# Real domain for dev/test boxes (sourced by scripts/dev-deploy.sh).
# <target>.$BASE_DOMAIN becomes the canonical host; sslip.io names stay as
# fallback aliases so dual-host cutover is free.
# DNS (GoDaddy): A test.rs-intel.com → 54.195.65.131  (added 2026-07-26)
BASE_DOMAIN=rs-intel.com
